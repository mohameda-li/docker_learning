services:
  web:
    build: .
    expose:
      - "5002"     # visible only inside Docker network
    depends_on:
      - redis
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379

  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data

volumes:
  redis-data:
